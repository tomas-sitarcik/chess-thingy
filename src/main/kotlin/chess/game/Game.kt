package chess.game

import chess.board.Piece

class Game(var board: Array<Array<Piece?>>, var multiplayer: Boolean = true) { // leaving multiplayer on by default, the AI is the last thing im gonna do

    fun start() {
        
    }
}